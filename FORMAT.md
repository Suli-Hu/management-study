# 知识库数据格式定义

本文档定义了通过网页端"+"按钮新增知识点、学者、学派时的文本粘贴格式。按此格式填写后粘贴到弹窗的文本框中即可创建。

---

## 1. 新增知识点（KP）

### 格式

```
标题: 价值链分析
en: Value Chain Analysis
学者: porter
年份: 1985
学派: io
---
波特在《竞争优势》（1985）中提出，把企业活动分解为<strong>主要活动</strong>和<strong>支援活动</strong>两大类...

◆意义——为企业内部分析提供了系统化工具
◆局限——对服务业的适用性有限
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `标题` | ✅ | 中文名称，如"价值链分析"。不要加括号里的英文/日文，那些放在`en`字段 |
| `en` | 否 | 英文名称，如"Value Chain Analysis"。显示在标题下方的副标题区 |
| `学者` | 否 | 学者的**key**（不是中文名），如`porter`、`simon`。可在学者目录页找到对应key。多个学者用逗号分隔：`cyert,march`。留空表示"常识"（无特定提出者） |
| `年份` | 否 | 提出年份，如`1985`。支持`1960s`格式。留空则不显示年份标签 |
| `学派` | ✅ | 学派的**key**（不是中文名），如`io`、`conflict`。可在学派全览页找到对应key。多个学派用逗号分隔：`io,rbv` |
| `---` | ✅ | 分隔符，单独一行，分隔元信息和正文 |
| 正文 | ✅ | `---`以下的全部内容作为知识点的正文body。最少10个字符 |

### 正文格式规范

正文遵循**三层信息架构**：

```
导语（1-2句，谁+何时+核心定义）
   ↓
核心内容（详细展开）
   ↓
评价标签（◆意义——xxx ◆局限——xxx）
```

#### 可用的格式标记

| 标记 | 用途 | 示例 |
|------|------|------|
| `<strong>关键词</strong>` | 加粗关键概念 | `<strong>有限理性</strong>` |
| `◆标题——内容` | 编号列表项（自动编号1,2,3...） | `◆意义——把决策分解为可研究的环节` |
| `<br>【组标题】<br>①内容<br>②内容` | 折叠手风琴（点击展开） | `<br>【四个独立的流】<br>①选择机会<br>②问题` |
| `<compare>列1\|\|列2\|\|列3</compare>` | 翻转对比卡片 | 见下方compare格式 |

#### 评价标签（必须放在正文末尾）

```
◆意义——这个理论的学术/实务贡献
◆局限——这个理论的不足或边界条件
```

- 必须用`◆`开头 + `——`（两个破折号）分隔
- 不能用冒号`：`代替破折号
- 意义和局限始终可见，不会被折叠

#### compare对比卡片格式

```
<compare>
标题1|关键词1|描述1|类型1|底部文字1|翻面详情1
||
标题2|关键词2|描述2|类型2|底部文字2|翻面详情2
</compare>
```

每列6个字段用`|`分隔，列与列之间用`||`分隔：

| 字段位置 | 含义 | 示例 |
|----------|------|------|
| 第1个 | 卡片标题 | 战略决策 |
| 第2个 | 关键词（大字显示，学派accent色） | 高层 |
| 第3个 | 描述（标题下方小字） | 非结构化·非定型 |
| 第4个 | 类型（不显示为pill，仅翻面标题用） | 高层管理者 |
| 第5个 | 底部文字（支持智能链接） | 资源配置与产品—市场定位 |
| 第6个 | 翻面详情（点击卡片翻转显示） | 安索夫从卡内基学派分化出... |

#### 手风琴折叠格式

```
<br>【组标题1】<br>①第一项<br>②第二项<br>③第三项
<br>【组标题2】<br>①第一项<br>②第二项
```

- `<br>【】`标记折叠组的标题，点击展开/收起
- 组内用`①②③`或`<strong>`加粗标记条目
- 评价标签（◆意义/◆局限）会自动跳出折叠组，始终可见

### 学者key查找方法

学者key定义在 `data.js` 的 `SCHOLARS` 对象中（搜索 `var SCHOLARS = {` 即可定位）。格式为：
```javascript
SCHOLARS = {
  porter: { name: "迈克尔·波特", en: "Michael E. Porter", ... },
  simon: { name: "赫伯特·西蒙", en: "Herbert Simon", ... },
  ...
}
```

key命名规则：英文小写姓氏（如 `porter`），同姓加首字母（如 `fisher_r`），日本学者用罗马音（如 `nonaka`）。多学者用逗号分隔：`cyert,march`。

如果学者不存在，需要先通过学者目录页的"+"按钮新增学者。

### 学派key查找方法
https://github.com/Suli-Hu/management-study/blob/main/data.js
学派key定义在 `data.js` 的 `DATA` 对象中（搜索 `var DATA = {` 即可定位）。格式为：
```javascript
DATA = {
  scientific: { title: "科学管理学派", en: "Scientific Management School", group: 3, ... },
  conflict:   { title: "冲突管理论", en: "Conflict Management", group: 2, ... },
  ...
}
```

每个学派的 `group` 字段决定它在全览页的分段位置（1-12，具体分组见本文档第3节的group分组编号表）。

如果学派不存在，需要先通过学派全览页的"+"按钮新增学派。

---

## 2. 新增学者

### 格式

```
key: porter
名字: 迈克尔·波特
en: Michael E. Porter
国籍: 美国
所属机构: 哈佛商学院
领域: 竞争战略
学派: io,rbv
---
波特是竞争战略理论的奠基人。提出五力模型、三大基本战略、价值链分析等核心框架，将产业组织经济学引入战略管理领域。
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `key` | ✅ | 英文小写标识符，全局唯一。通常用姓氏小写：`porter`、`simon`。日本学者用罗马音：`nonaka`。有同姓时加首字母：`fisher_r`、`walton_r` |
| `名字` | ✅ | 中文全名，如"迈克尔·波特"。中间用`·`（中文间隔号）连接 |
| `en` | 否 | 英文全名，如"Michael E. Porter" |
| `国籍` | 否 | 如"美国"、"英国"、"日本" |
| `所属机构` | 否 | 主要所属大学/机构，如"哈佛商学院" |
| `领域` | 否 | 研究领域，用`·`分隔多个：`竞争战略 · 产业分析` |
| `学派` | 否 | 所属学派的key，逗号分隔。用于学者页面的学派标签显示 |
| `---` | ✅ | 分隔符 |
| 贡献描述 | 否 | `---`以下的文字作为学者的contribution（学术贡献描述），显示在学者详情页 |

### key命名规范

| 情况 | 规则 | 示例 |
|------|------|------|
| 普通姓氏 | 姓氏小写 | `porter`、`simon`、`march` |
| 同姓不同人 | 姓氏_名首字母 | `fisher_r`（Roger Fisher）、`cohen_w`（Wesley Cohen） |
| 日本学者 | 姓氏罗马音 | `nonaka`、`takeuchi` |
| 有连字符的姓 | 保持连字符 | `von_hippel` |

**查重**：新增前先在 `data.js` 的 `SCHOLARS` 对象中搜索确认该key不存在，避免重复创建。

---

## 3. 新增学派

### 格式

```
key: new_school
标题: 新学派名
en: New School Name
group: 7
颜色: #007AFF
---
学派的概述文字，介绍这个学派研究什么、核心主张是什么。
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `key` | ✅ | 英文小写标识符，全局唯一，用下划线连接：`conflict`、`decision_theory`、`org_learning` |
| `标题` | ✅ | 中文学派名，如"冲突管理论" |
| `en` | 否 | 英文学派名，如"Conflict Management" |
| `group` | ✅ | 所属分组编号（1-12），决定在学派全览页的哪个段落中显示 |
| `颜色` | 否 | 学派主题色（HEX格式），默认`#007AFF`。OB类用`#34C759`，OT类用`#FF9500`，SM类用`#007AFF` |
| `---` | ✅ | 分隔符 |
| 学派简介 | 否 | `---`以下的文字作为学派的summary |

### group分组编号

| 编号 | 段名 | 学科 | 主题色 |
|------|------|------|--------|
| 1 | 个体的世界：人是怎么想和感受的 | OB | #34C759 |
| 2 | 人与人之间：群体这种现象 | OB | #34C759 |
| 3 | 古典组织论：组织作为机器 | OT | #FF9500 |
| 4 | 近代组织论：组织作为生命体 | OT | #FF9500 |
| 5 | 人材与雇用：组织如何管理人 | HR | #FF9500 |
| 6 | 战略论 内部视点 | SM | #007AFF |
| 7 | 战略论 外部视点 | SM | #007AFF |
| 8 | 组织与环境：超越组织的世界 | OT | #FF9500 |
| 10 | 战略形成过程（HOW） | SM | #007AFF |
| 11 | 斯科特四象限 | OT | #FF9500 |
| 12 | 院校学派 | — | #007AFF |

---

## 4. 注意事项

### 创建顺序
如果新增KP引用了新学者或新学派，必须**先创建学者/学派，再创建KP**。否则会被校验拦截。

### 日文版
通过网页端新增的内容**不含日文翻译**。日文版在定期合并到代码库时由Claude添加。

### 删除规则
- 网页端可以删除**未被其他地方引用**的KP
- 如果KP被其他KP的正文引用，需要在Claude Code中操作
- 学派和学者的删除同理

### 数据持久化
新增/删除的数据保存在Cloudflare KV中，页面加载时自动合并。定期由Claude将KV增量合并回代码库并部署。
